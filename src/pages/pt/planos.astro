---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import AgencyOffers from '../../components/AgencyOffers.astro';
import { getAgency } from '../../data/agency';
const c = getAgency('pt');
---
<Layout title={c.nav[3] + ' — Eddie Miller Agency'} description={c.description} ogTitle={c.nav[3] + ' — Eddie Miller Agency'} ogDescription={c.description} skipFaqSchema>
  <Header /><main id="main-content"><AgencyOffers /></main><Footer />
</Layout>
